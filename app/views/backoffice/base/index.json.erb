<%= sanitize ({ collection: collection.decorate.as_json,
                total_pages: collection.total_pages,
                current_page: collection.current_page,
                total_count: collection.total_count,
                per_page: collection.limit_value }).to_json %>
